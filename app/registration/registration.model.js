(function(angular) {
    'use strict';
    
    angular
        .module('myApp.registration')
        .factory('RegistrationModel', registrationModel);
    
    function registrationModel () {
        
    }
})(window.angular);